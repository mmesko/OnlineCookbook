
using System;

namespace OnlineCookbook.Common.Filters
{
    public class CategoryFilter : GenericFilter
    {
         public CategoryFilter(int pageNumber, int pageSize)
            : base(pageNumber, pageSize)
        {
            
        }
        
    }
}