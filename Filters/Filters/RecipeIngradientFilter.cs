using System;
using System.Text;

namespace OnlineCookbook.Common.Filters
{
    public class RecipeIngradientFilter : GenericFilter
    {
        public RecipeIngradientFilter(int pageNumber, int pageSize)
            : base(pageNumber, pageSize)
        {
            
        }
    }
}
